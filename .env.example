# Local development configuration only.
# Copy this file to .env and fill the values locally.
# .env is ignored by Git and must never be committed.

VITE_SUPABASE_URL=""
VITE_SUPABASE_ANON_KEY=""

# Legacy browser-side AI integration.
# Do not use a sensitive production key in a public GitHub Pages build.
VITE_GOOGLE_API_KEY=""
